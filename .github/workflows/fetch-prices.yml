name: Fetch Prices

on:
  schedule:
    - cron: '30 6 * * 1-5'  # 06:30 UTC Mon–Fri — after fund prices published and ETF markets closed
  workflow_dispatch:
    inputs:
      deploy_only:
        description: 'Skip price fetch and redeploy current data'
        type: boolean
        default: false

permissions:
  contents: write             # needed to commit docs/data/ back to the repo
  pages: write                # needed to deploy to GitHub Pages
  id-token: write             # needed for Pages OIDC authentication

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        if: ${{ !inputs.deploy_only }}
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        if: ${{ !inputs.deploy_only }}
        run: pip install requests beautifulsoup4

      - name: Create docs/data directory
        if: ${{ !inputs.deploy_only }}
        run: mkdir -p docs/data

      - name: Fetch prices
        if: ${{ !inputs.deploy_only }}
        run: python scripts/fetch_prices.py

      - name: Commit and push data
        if: ${{ !inputs.deploy_only }}
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/data/
          git diff --cached --quiet || git commit -m "chore: update fund prices $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/

  deploy:
    needs: fetch                # only runs after fetch job succeeds
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:

      - name: Deploy to GitHub Pages
        id: deploy
        uses: actions/deploy-pages@v4